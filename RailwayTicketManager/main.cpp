#include "utils.h"
#include "user_interface_backend.h"

int main() {
    //bool debug=false;

    //if(arg=="debug") debug=true;

    while (true) {
        // THE loop
        read_user_data("deez nuts");
    }
}